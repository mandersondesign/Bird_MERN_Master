/**
 * javascript round number to nearest 0.5
 * @param  {number} num source data
 * @return {number}     return round number
 */
const roundHalf = num => Math.round(num * 2) / 2

export default roundHalf
