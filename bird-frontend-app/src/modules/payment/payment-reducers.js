import { } from './payment-constants'

export const paymentInitialState = {
  loading: false,
}

export const paymentReducer = (state = paymentInitialState, { type, payload }) => {
  switch (type) {
    default:
      return state
  }
}
