import { connect } from 'react-redux'
import { CompleteOnBoarding } from 'components/dashboard/Pages'

const mapStateToProps = () => ({})

const mapDispatchToProps = () => ({
})

export default connect(
  mapStateToProps,
  mapDispatchToProps,
)(CompleteOnBoarding)
